spring.application.name=dropboxstreamer
server.port=8080

# Local dev: reads from backend/.env via spring-dotenv
# Docker: falls back to system env var set by docker-compose
dropbox.access-token=${env.DROPBOX_ACCESS_TOKEN:${DROPBOX_ACCESS_TOKEN}}
